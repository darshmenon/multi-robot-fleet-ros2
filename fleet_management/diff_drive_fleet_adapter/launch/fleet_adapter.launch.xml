<?xml version='1.0' ?>

<launch>

  <arg name="use_sim_time" default="true" description="Use the /clock topic for time to sync with simulation"/>
  <arg name="config_file" default="$(find-pkg-share diff_drive_fleet_adapter)/config/diff_drive_robot_config.yaml" description="The config file that provides important parameters for setting up the adapter"/>
  <arg name="nav_graph_file" default="$(find-pkg-share diff_drive_fleet_adapter)/config/nav_graphs/0.yaml" description="The graph that this fleet should use for navigation"/>
  <arg name="server_uri" default="" description="The URI of the api server to transmit state and task information."/>

  <!-- Fleet adapter: talks directly to diff_drive_robot's Nav2 stack, no
       separate REST fleet-manager process is needed. -->
  <node pkg="diff_drive_fleet_adapter"
        exec="fleet_adapter"
        args="-c $(var config_file) -n $(var nav_graph_file)"
        output="both">

    <param name="use_sim_time" value="$(var use_sim_time)"/>
    <param name="server_uri" value="$(var server_uri)"/>
  </node>

</launch>
